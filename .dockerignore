# Ignore everything by default
*

# Allow runtime inputs (example configs only; never bake local secrets into image)
!config/
!config/**
config/local.toml
config/production.toml
!target/release/arcgis-mcp-oauth
!Dockerfile
!.dockerignore
